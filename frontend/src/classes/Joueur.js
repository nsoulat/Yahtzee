
export default class Joueur {
    Id = null;
    Name = null;

    constructor(id, name) {
        this.Id = id;
        this.Name = name;
        // console.log(`id: ${this.Id}, name:${this.Name}, created`);
    }
}